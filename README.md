Simple chat application with using React on a Frontend and Spring Boot on the Backend.

To compile changed backend code, use `./mvnw clean install`.

To run the server, use `./mvnw spring-boot:run`.


